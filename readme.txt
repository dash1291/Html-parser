This HTML/XML parser converts an HTML/XML document into a tree-like data structure using C. Moreover, some code is also written for traversing the tree and displaying the contents on a win32 based GUI.

